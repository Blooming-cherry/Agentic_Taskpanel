# TaskPanel Web —— 使用说明与验收清单

本地并行 AI 任务面板:多个 agent 任务各自独立上下文并行运行,支持普通问答(Chat)与 OCR 代码审查(Project)。Web 三栏 UI,复刻 Codex 桌面端体验。后端经 Anthropic 兼容端点连接你的 DeepSeek 映射。

---

## 一、保存路径

| 内容 | 路径 |
|---|---|
| 项目根目录 | `C:\Users\admin\projects\taskpanel` |
| Python 源码 | `src\taskpanel\core\`(LLM/AgentLoop/工具/OCR)、`src\taskpanel\store\`(持久化/worktree)、`src\taskpanel\web\`(FastAPI/Manager) |
| 前端源码 | `frontend\src\`(React 组件 + api.js) |
| 前端构建产物(已入库) | `src\taskpanel\web\static\`(浏览器直接可访问) |
| 运行数据目录 | `~\.taskpanel\` |
| ├─ 每任务数据 | `~\.taskpanel\tasks\<任务id>\messages.jsonl`(对话历史)、`events.jsonl`(事件流)、`meta.json`(状态/元信息) |
| ├─ 认证 token | `~\.taskpanel\.auth_token`(首次启动生成,权限 0600) |
| ├─ 全局事件序号 | `~\.taskpanel\seq`(断线补齐用全局单调 seq) |
| ├─ git worktree | `~\.taskpanel\worktrees\<随机id>\`(project 任务隔离工作区) |
| └─ 共享记忆(默认关) | `~\.taskpanel\shared\` |
| 配置文件 | 项目根 `.env`(复制 `.env.example` 填写) |
| 设计/实现文档 | `docs\superpowers\specs\`、`docs\superpowers\plans\` |

## 二、快速开始

前置:Python ≥ 3.11、Node ≥ 18、Git ≥ 2.41、`uv`。

```bash
# 1. 前端构建(产物自动进 src/taskpanel/web/static)
cd frontend && npm install && npm run build && cd ..

# 2. 配置后端(你的 DeepSeek↔Anthropic 映射)
cp .env.example .env
#    在 .env 里填 TASKPANEL_LLM_BASE_URL / TASKPANEL_LLM_API_KEY / TASKPANEL_LLM_MODEL
#    以及 OCR_LLM_URL / OCR_LLM_TOKEN / OCR_LLM_MODEL

# 3. 安装 OCR 依赖(open-code-review,用于代码审查任务)
npm i -g @alibaba-group/open-code-review

# 4. 启动
uv run python run.py
#    浏览器打开 http://127.0.0.1:8470
```

启动时自动完成:生成/读取认证 token、扫描恢复历史任务、探测模型 `tool_use` 支持度(不支持则任务降级为纯文本问答)、后台 worktree 清理。

## 三、面板使用

**新建任务**:`Ctrl+N` 或左侧 `+ 新建任务`。
- **Chat** — 不绑定仓库,纯问答/研究。
- **Project** — 绑定仓库路径,可读写文件、跑脚本、做 OCR review;勾选 **worktree 隔离**后任务在独立 git worktree 中运行,同仓库多任务并行互不污染。

**任务列表**:按 Project / Chat 分组,状态徽标:`queued` 排队 / `running` 运行 / `done` 完成 / `error` 出错 / `stopped` 终止 / `paused` 打断。

**操作**:
- 点击任务切换查看,不中断运行;每个任务独立上下文,互不串台。
- 中栏底部输入框 = 对该任务 follow-up 续聊(运行中任务会返回 busy)。
- `停止` 打断当前生成(上下文保留);`删除` 移除任务。
- 右栏 **Review**(仅 Project):点 `运行 Review` 触发 `ocr review`,展示结构化发现(High/Medium/Low 筛选),点 `展开 diff` 看锚点行 ±8 行上下文;`Esc` 折叠/展开右栏。
- 浏览器刷新/断线自动重连,事件按全局 seq 补齐,不丢消息。

## 四、配置项(`.env`)

| 变量 | 默认 | 说明 |
|---|---|---|
| `TASKPANEL_LLM_BASE_URL` | — | LLM 端点(DeepSeek 映射 base) |
| `TASKPANEL_LLM_API_KEY` | — | API key |
| `TASKPANEL_LLM_MODEL` | — | 模型名 |
| `TASKPANEL_MAX_PARALLEL` | 空=不限 | 并发任务上限 |
| `TASKPANEL_BIND_HOST` | `127.0.0.1` | 仅本机,勿改 0.0.0.0 |
| `TASKPANEL_BIND_PORT` | `8470` | 端口 |
| `TASKPANEL_MAX_TOOL_ROUNDS` | `20` | 单任务工具循环上限 |
| `TASKPANEL_LLM_TIMEOUT` | `60` | LLM 请求超时(秒) |
| `TASKPANEL_MAX_RETRIES` | `3` | 失败重试(含 SDK→raw 回退) |
| `TASKPANEL_WORKTREE_CLEANUP` | `true` | worktree 自动清理 |
| `TASKPANEL_MAX_WORKTREES` | `5` | 保留的 stale worktree 数 |
| `TASKPANEL_OCR_TIMEOUT` | `1800` | OCR 子进程超时(秒) |
| `TASKPANEL_DIFF_CONTEXT` | `8` | Review diff 上下文行数 |
| `TASKPANEL_DATA_DIR` | `~/.taskpanel` | 数据目录 |
| `OCR_LLM_URL` / `OCR_LLM_TOKEN` / `OCR_LLM_MODEL` | — | OCR 审查用 LLM(Anthropic 兼容) |
| `OCR_USE_ANTHROPIC` | `true` | OCR 走 Anthropic 协议 |

## 五、架构简介

- **core** — `LLMClient` 双实现(AnthropicSDK + RawHTTP 兜底)与能力探测、`AgentLoop`(每任务独立消息历史 + 工具循环)、Task 状态机、工具集(读写文件/bash,工作区约束)、OCR 子进程包装。
- **store** — JSONL 消息/事件持久化(每轮即写,崩溃可恢复)、全局单调 seq、git worktree 隔离与清理。
- **web** — FastAPI + WebSocket(事件推送 + 断线按 seq 补齐)、token 鉴权(无 CORS)、React 三栏 UI。

## 六、测试与已验证情况

```bash
uv run pytest -q        # 64 通过
cd frontend && npm run build   # 构建成功
```

**已自动化验证**(64 个 pytest + 构建 + 服务端冒烟):
- 配置/任务模型/LLM 客户端/RawHTTP SSE 解析/工具工作区约束/AgentLoop 工具循环
- JSONL 持久化 + 全局 seq + 崩溃恢复(running→paused 保留上下文)
- 工具探测与降级、SDK→raw 协议回退、重试脏消息回滚
- 鉴权(token 401/4401)、REST 全端点、WS 回放/直播/断线补齐
- **安全**:get_context 路径穿越防护、delete_task 非法 id 防护、工具越权拒绝
- 多工具轮单消息分组(Anthropic 协议)、运行中删除不复活、follow-up busy 防并发、worktree 清理保护活跃任务
- 前端三栏 UI、Review 面板、重连去重(构建产物含对应逻辑)

**尚未验证**(需真实 DeepSeek key / 真实 `ocr` CLI,本环境无):
- 真实 LLM 对话链路与工具调用产出(含降级路径的端到端)
- 真实 `ocr review` 的发现产出与 Review 面板渲染
- ≥3 任务真实并行的长时行为

## 七、已知限制(二期候选)

- 单进程、单用户、仅本机;`token_count` 未统计(状态栏 Token 恒 0)。
- 无 Automations 定时任务 / Plugins / Cloud / Electron 桌面壳(已列入设计延迟项)。
- 启动时有 17 条 `on_event`/TestClient 弃用警告(不影响功能)。
- `frontend/package-lock.json` 的 registry 指向 npmmirror(区域可用,跨区可改 `.npmrc`)。
- 共享记忆(`~/.taskpanel/shared/`)已实现但默认不启用。
